# TEMPORARY. Delete this entry when the JustFiles package is more than 24 hours old.
minimumReleaseAgeExclude:
  - '@justfiles/app@0.9.0'
  - '@types/node@26.3.0'
# esbuild ships a platform binary and needs its install script.
allowBuilds:
  esbuild: true
